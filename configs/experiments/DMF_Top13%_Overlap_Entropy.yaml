# Test parameter sweep - minimal example
simulation:
  nb_steps: 100000  # Shorter for testing
  obj_rate: 3.44
  with_decay: false
  with_plasticity: false  
  TR: 2.4
  flp: 0.01
  fhp: 0.1
  burnout: 8
  G: 2.05
  alpha: 0.72
  over: 
    seed:  # or 'param'
      fun: "np.arange"      
      args: [1, 101, 1]      
  parallel: true  
  averaged: false
 
data:  
  test_mode: false  
  max_subjects_test: 1
  sc_root: "Averaged_SCs/aal"  
output:
  save_full_outputs: true  
  results_dir: "AAL_HDMF_PAPER"
  observables:
    - name: gamma_entropy
      signal: rates
  
experiment:
  description: "100 repetitions where Static DMF is in the top 13% overlap between FC and FCD, calculate entropy "
  version: "1.0"
